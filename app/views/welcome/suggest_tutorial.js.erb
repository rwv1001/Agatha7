const tutorialScheduleDiv = document.getElementById("<%= "tutorial_schedule_div" %>");
if (tutorialScheduleDiv) {
  tutorialScheduleDiv.innerHTML = `<%= j render(:partial => 'shared/suggested_tutorial', :object => suggested_tutorial_schedule) %>`;
}
unwait();
