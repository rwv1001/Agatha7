var container = document.getElementById('<%= "group_filters_#{table_name}" %>');
if (container) {
  container.innerHTML = `<%= j render(:partial => 'shared/group_filters', :object => group_filters) %>`;
}
unwait();
