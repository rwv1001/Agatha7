var formatDiv = document.getElementById('<%= "format_controller_div" %>');
if (formatDiv) {
  formatDiv.innerHTML = `<%= j render(:partial => 'shared/format_controller', :object => format_controller) %>`;
}
resizeFormat();
unwait();
